<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Teko:wght@400;600&family=Inter:wght@400;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">

<link rel="stylesheet" href="{{ 'hero-glitch.css' | asset_url }}">
<link rel="stylesheet" href="{{ 'products.css' | asset_url }}">

<script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.29/bundled/lenis.min.js"></script>
<script type="module" src="https://cdn.shopify.com/storefront/web-components.js"></script>
<script type="module" src="{{ 'carousel.js' | asset_url }}"></script>

<style>
    /* Smooth Scroll */
    html.lenis { height: auto; }
    .lenis.lenis-smooth { scroll-behavior: auto; }

    /* Video Sections */
    .full-video-section {
        position: relative; height: 80vh; overflow: hidden;
        display: flex; align-items: center; justify-content: center;
    }
    .full-video-bg {
        position: absolute; width: 100%; height: 100%; object-fit: cover;
        filter: grayscale(100%) contrast(1.2); opacity: 0.6;
    }
    .overlay-content {
        position: relative; z-index: 2; text-align: center;
    }
    .stat-grid {
        display: flex; gap: 60px; margin-top: 40px;
    }
    .stat-item h3 {
        font-family: 'Teko', sans-serif; font-size: 80px; margin: 0;
        color: var(--color-neon-green); line-height: 1;
    }
    .stat-item p {
        font-family: 'JetBrains Mono', monospace; font-size: 12px; color: #FFF;
    }
</style>

<div class="volt-theme">

<script>
    const lenis = new Lenis();
    function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
    requestAnimationFrame(raf);
</script>

<section class="hero-carousel">
    <div class="carousel-container">
        
        {% for block in section.blocks %}
          {% if block.type == 'slide' %}
            <div class="carousel-slide {% if forloop.first %}active{% endif %}" {{ block.shopify_attributes }}>
                {% if block.settings.video != blank %}
                  <video class="carousel-video" {% if forloop.first %}autoplay{% endif %} loop muted playsinline src="{{ block.settings.video.sources[1].url }}"></video>
                {% endif %}
                <div class="carousel-content-split">
                    <div class="hero-top-left">
                        <div class="carousel-label" {% if block.settings.accent_color != blank %}style="color:{{ block.settings.accent_color }}"{% endif %}>{{ block.settings.label }}</div>
                        <h1 class="carousel-headline">{{ block.settings.headline }}</h1>
                    </div>
                    <div class="hero-bottom-right">
                        {% if block.settings.tagline != blank %}
                          <p class="carousel-tagline">{{ block.settings.tagline }}</p>
                        {% endif %}
                        {% if block.settings.description != blank %}
                          <p class="carousel-description">{{ block.settings.description }}</p>
                        {% endif %}
                        {% if block.settings.price != blank %}
                          <div class="carousel-price">
                              <span class="price-amount">{{ block.settings.price }}</span>{% if block.settings.price_unit != blank %}<span class="price-unit">/ {{ block.settings.price_unit }}</span>{% endif %}
                          </div>
                        {% endif %}
                        <div class="carousel-cta">
                            <button class="btn-primary" {% if block.settings.accent_color != blank %}style="background:{{ block.settings.accent_color }}; color:#FFF;"{% endif %} onclick="scrollToProducts()">{{ block.settings.button_text | default: 'INITIATE ORDER' }}</button>
                        </div>
                    </div>
                </div>
            </div>
          {% endif %}
        {% endfor %}

    </div>

    <button class="carousel-nav carousel-nav--prev" onclick="prevSlide()">&larr;</button>
    <button class="carousel-nav carousel-nav--next" onclick="nextSlide()">&rarr;</button>
    <div class="carousel-pagination">
        {% for block in section.blocks %}
          {% if block.type == 'slide' %}
            <button class="pagination-dot {% if forloop.first %}active{% endif %}" onclick="goToSlide({{ forloop.index0 }})"></button>
          {% endif %}
        {% endfor %}
    </div>
</section>

<section class="full-video-section">
    {% if section.settings.stats_video != blank %}
      <video class="full-video-bg" autoplay loop muted playsinline src="{{ section.settings.stats_video.sources[1].url }}"></video>
    {% elsif section.settings.stats_video_url != blank %}
      <video class="full-video-bg" autoplay loop muted playsinline src="{{ section.settings.stats_video_url }}"></video>
    {% endif %}
    <div class="overlay-content">
        <h2 class="carousel-headline" style="font-size: 60px;">{{ section.settings.stats_heading | default: 'PERFORMANCE METRICS' }}</h2>
        <div class="stat-grid">
            {% for block in section.blocks %}
              {% if block.type == 'stat' %}
                <div class="stat-item" {{ block.shopify_attributes }}>
                    <h3>{{ block.settings.stat_value }}<span style="font-size:30px">{{ block.settings.stat_unit }}</span></h3>
                    <p>{{ block.settings.stat_label }}</p>
                </div>
              {% endif %}
            {% endfor %}
        </div>
    </div>
</section>

<section id="products" class="products-section">
    <div class="container-volt">
        <div class="section-header">
            <div>
                <div class="section-label">{{ section.settings.products_label | default: '// CATALOG_V1.0' }}</div>
                <h2 class="section-headline">{{ section.settings.products_heading | default: 'SELECT FUEL' }}</h2>
            </div>
        </div>

        <div class="products-grid">
            {% if section.settings.storefront_domain != blank and section.settings.storefront_token != blank %}
              <shopify-store store-domain="{{ section.settings.storefront_domain }}" public-access-token="{{ section.settings.storefront_token }}" country="{{ section.settings.storefront_country | default: 'US' }}">
                  <shopify-cart id="cart"></shopify-cart>
                  <shopify-list-context type="product" query="products" first="{{ section.settings.products_count | default: 3 }}">
                      <template>
                          <div class="product-card">
                              <a shopify-attr--href="'/products/' + product.handle" style="text-decoration:none;">
                                  <img src="https://images.unsplash.com/photo-1622543925917-095a35c9704d?q=80&w=600&auto=format&fit=crop" class="product-card__image" alt="Can">
                                  <div class="product-card__content">
                                      <div class="product-card__title"><shopify-data query="product.title"></shopify-data></div>
                                      <div class="product-card__price"><shopify-money query="product.selectedOrFirstAvailableVariant.price"></shopify-money></div>
                                  </div>
                              </a>
                          </div>
                      </template>
                  </shopify-list-context>
              </shopify-store>
            {% endif %}

            {% for block in section.blocks %}
              {% if block.type == 'product_card' %}
                <div class="product-card" {{ block.shopify_attributes }}>
                    {% if block.settings.product_image != blank %}
                      <img src="{{ block.settings.product_image | image_url: width: 600 }}" class="product-card__image">
                    {% endif %}
                    <div class="product-card__content">
                        <div class="product-card__title">{{ block.settings.product_title }}</div>
                        <div class="product-card__price">{{ block.settings.product_price }}</div>
                    </div>
                </div>
              {% endif %}
            {% endfor %}
        </div>
    </div>
</section>

</div>

{% schema %}
{
  "name": "Volt Home",
  "max_blocks": 20,
  "settings": [
    {
      "type": "header",
      "content": "Performance Metrics Section"
    },
    {
      "type": "video",
      "id": "stats_video",
      "label": "Stats Background Video"
    },
    {
      "type": "url",
      "id": "stats_video_url",
      "label": "Stats Video URL (fallback if no upload)"
    },
    {
      "type": "text",
      "id": "stats_heading",
      "label": "Stats Section Heading",
      "default": "PERFORMANCE METRICS"
    },
    {
      "type": "header",
      "content": "Products Section"
    },
    {
      "type": "text",
      "id": "products_label",
      "label": "Products Label",
      "default": "// CATALOG_V1.0"
    },
    {
      "type": "text",
      "id": "products_heading",
      "label": "Products Heading",
      "default": "SELECT FUEL"
    },
    {
      "type": "text",
      "id": "storefront_domain",
      "label": "Storefront Domain",
      "default": "palashstor.myshopify.com"
    },
    {
      "type": "text",
      "id": "storefront_token",
      "label": "Storefront Access Token",
      "default": "7e706483ff729f77a768614acd45fdc0"
    },
    {
      "type": "text",
      "id": "storefront_country",
      "label": "Storefront Country",
      "default": "US"
    },
    {
      "type": "range",
      "id": "products_count",
      "label": "Products to Show",
      "min": 1,
      "max": 12,
      "step": 1,
      "default": 3
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Hero Slide",
      "settings": [
        {
          "type": "video",
          "id": "video",
          "label": "Background Video"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Slide Label",
          "default": "FORMULA 01"
        },
        {
          "type": "text",
          "id": "headline",
          "label": "Headline",
          "default": "SYSTEM<br>OVERLOAD"
        },
        {
          "type": "text",
          "id": "tagline",
          "label": "Tagline",
          "default": "Original Citrus Blend"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "200mg Caffeine. Zero Sugar. The industry standard for high-performance neural activation."
        },
        {
          "type": "text",
          "id": "price",
          "label": "Price",
          "default": "$34"
        },
        {
          "type": "text",
          "id": "price_unit",
          "label": "Price Unit",
          "default": "12 Pack"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button Text",
          "default": "INITIATE ORDER"
        },
        {
          "type": "color",
          "id": "accent_color",
          "label": "Accent Color"
        }
      ]
    },
    {
      "type": "stat",
      "name": "Stat Item",
      "settings": [
        {
          "type": "text",
          "id": "stat_value",
          "label": "Value",
          "default": "200"
        },
        {
          "type": "text",
          "id": "stat_unit",
          "label": "Unit",
          "default": "MG"
        },
        {
          "type": "text",
          "id": "stat_label",
          "label": "Label",
          "default": "CAFFEINE ANHYDROUS"
        }
      ]
    },
    {
      "type": "product_card",
      "name": "Product Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "product_image",
          "label": "Product Image"
        },
        {
          "type": "text",
          "id": "product_title",
          "label": "Product Title",
          "default": "SYSTEM OVERLOAD"
        },
        {
          "type": "text",
          "id": "product_price",
          "label": "Product Price",
          "default": "$34.00"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Volt Home",
      "settings": {
        "stats_video_url": "https://videos.pexels.com/video-files/853813/853813-hd_1920_1080_25fps.mp4",
        "stats_heading": "PERFORMANCE METRICS",
        "products_label": "// CATALOG_V1.0",
        "products_heading": "SELECT FUEL",
        "storefront_domain": "palashstor.myshopify.com",
        "storefront_token": "7e706483ff729f77a768614acd45fdc0",
        "storefront_country": "US",
        "products_count": 3
      },
      "blocks": [
        {
          "type": "slide",
          "settings": {
            "label": "FORMULA 01",
            "headline": "SYSTEM<br>OVERLOAD",
            "tagline": "Original Citrus Blend",
            "description": "200mg Caffeine. Zero Sugar. The industry standard for high-performance neural activation.",
            "price": "$34",
            "price_unit": "12 Pack",
            "button_text": "INITIATE ORDER"
          }
        },
        {
          "type": "slide",
          "settings": {
            "label": "FORMULA 02",
            "headline": "CRYOGENIC<br>FREEZE",
            "tagline": "Blue Raspberry Ice",
            "description": "Enhanced with Nootropics for laser focus during extended gaming or coding sessions.",
            "price": "$34",
            "price_unit": "12 Pack",
            "button_text": "INITIATE ORDER"
          }
        },
        {
          "type": "slide",
          "settings": {
            "label": "FORMULA 03",
            "headline": "NEON<br>DRIVE",
            "tagline": "Grape Electrolyte",
            "description": "Rapid hydration matrix designed for physical endurance and recovery.",
            "button_text": "INITIATE ORDER"
          }
        },
        {
          "type": "stat",
          "settings": {
            "stat_value": "200",
            "stat_unit": "MG",
            "stat_label": "CAFFEINE ANHYDROUS"
          }
        },
        {
          "type": "stat",
          "settings": {
            "stat_value": "0",
            "stat_unit": "G",
            "stat_label": "SUGAR CONTENT"
          }
        },
        {
          "type": "stat",
          "settings": {
            "stat_value": "100",
            "stat_unit": "%",
            "stat_label": "FOCUS RETENTION"
          }
        },
        {
          "type": "product_card",
          "settings": {
            "product_title": "SYSTEM OVERLOAD",
            "product_price": "$34.00"
          }
        },
        {
          "type": "product_card",
          "settings": {
            "product_title": "CRYOGENIC FREEZE",
            "product_price": "$34.00"
          }
        },
        {
          "type": "product_card",
          "settings": {
            "product_title": "NEON DRIVE",
            "product_price": "$34.00"
          }
        }
      ]
    }
  ]
}
{% endschema %}
